{
  "name": "enloopd",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Answers the Enloop extension's mid-run questions and runs its commands with no active agent session to keep open — the answering runs on the Claude API, or an installed Claude Code / Codex driven headlessly.",
  "bin": {
    "enloopd": "./dist/enloopd.mjs"
  },
  "scripts": {
    "build": "vite build",
    "typecheck": "NODE_OPTIONS=--max-old-space-size=8192 tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@tcm/shared": "*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vite": "^8.1.3"
  }
}
