# Fixture Shop

The app the Enloop evals author cases against — two screens, a coupon
banner and a coupon form, with routes in `src/Routing.tsx` and test handles
in the components. Not a real product; see `../README.md` for how the
matrix runs.
